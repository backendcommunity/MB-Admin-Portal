/**
 * The activation-walkthrough checkbox is built and shipped disabled: the
 * three-months-free flow it describes is not confirmed live. Flip this to
 * true to enable it — no API change, no template re-upload.
 */
export const ACTIVATION_VIDEO_AVAILABLE = false;
